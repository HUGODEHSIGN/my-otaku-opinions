<div class="d-flex flex-column gap-2">
    <div class="row row-cols-1 row-cols-md-3 g-3">
        {{#each animeData as |anime|}}
        <div class="col">
            {{> animeCard}}
        </div>
        {{/each}}
    </div>
</div>